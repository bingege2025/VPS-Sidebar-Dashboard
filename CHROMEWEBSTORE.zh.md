<!--
Chrome Web Store「详细说明」— 简体中文版。纯文本，可直接粘贴。
Chrome 网上应用店不渲染 HTML / Markdown，请保留换行。

用法：
1. 开发者后台 → 你的商品 → Store listing（商品详情）标签页
2. 页面顶部的语言下拉选择「简体中文(zh_CN)」
3. 只复制下方正文（不要包含本注释）粘贴到 Detailed description

注意：语言下拉里必须出现「简体中文」才可填写。该项来自扩展包内的
_locales/zh_CN 目录 —— 目录名必须是 zh_CN，写 zh 不会被 Chrome 识别。

如后台该语言下也提供「名称 / 简短说明」字段，建议填：
  名称：VPS Dashboard — Multi-Provider VPS Manager（保持与 manifest.json 一致，45 字符内）
  简短说明：跨多家服务商管理 VPS 服务器——状态、资源、电源操作与到期提醒，尽在浏览器侧边栏。
-->

VPS Dashboard

一款轻量、注重隐私的 Chrome 扩展，用于跨多家服务商和控制面板管理 VPS 服务器。

直接在浏览器侧边栏管理你的服务器——查看状态、资源占用、带宽，并执行电源操作，无需逐个打开各家服务商的后台。

本地优先。无需账号。你的 API 凭据永不离开浏览器。

为什么选择 VPS DASHBOARD？

- 一个面板管理所有 VPS
- 从 Chrome 侧边栏快速访问
- 本地优先架构——服务器数据与凭据只留在你的设备上。仅会向 Google Analytics 发送匿名的、不可识别身份的使用事件，你可随时在设置中关闭
- 支持多家服务商
- 为 VPS 用户而做，不是企业级监控

VPS Dashboard 专注服务商层面的管理，而非操作系统级别的监控。它是对专业监控工具的补充，而不是替代。

支持的服务商

支持连接多家 VPS 服务商与控制面板，部分为稳定版、部分为实验性——每种接入方式见扩展内的配置教程。

主要功能

多服务商支持
通过专用 API 驱动连接多家 VPS 服务商，在一个界面中统一管理。

服务商侧资源视图
查看状态、内存、磁盘、带宽、IP、主机名、操作系统等——全部来自服务商 API。

智能电源控制
在弹窗中直接开机、关机、重启（取决于服务商是否支持）。确认对话框可防止误操作。电源操作会适配各服务商的服务器状态，例如自动识别 pending、stopping 等过渡状态。

批量操作
一次性刷新、重启或关闭多台服务器，逐台返回执行结果。

标签与搜索
用自定义标签整理服务器，支持筛选和搜索，快速定位。

默认服务器
设置一台默认服务器，打开扩展时自动加载。

隐私模式
一键模糊 IP、主机名等敏感信息，截图和屏幕共享时更安全。

配置导入 / 导出
将配置导出为 JSON 备份，更换浏览器或设备时可导入。

到期提醒
扩展每 6 小时检查一次服务器，在到期前 30、7、3 天通知你；已过期的服务器每天提醒一次，直到续费。含全局总开关与单台服务器单独关闭。

自动获取到期日
对于提供账单日期的服务商，会自动拉取到期日。手动填写始终优先覆盖。
API 日期可能不准确，请自行核实。

日历导出（.ics）
将服务器导出为 .ics 日历文件，并在每个阈值设置提醒。可导入 Google 日历、Apple 日历、Outlook 或任何符合标准的日历应用。

深色模式
内置亮色与深色主题。

多语言
English · 中文 · Deutsch · Français · Русский

隐私

- 本地处理——服务器信息在你的浏览器中运行；仅向 Google Analytics 发送匿名统计数据
- 无需账号——零注册
- 不收集 API Key、凭据或任何敏感数据
- 直连服务商 API——服务器请求直接发往你的服务商；唯一的第三方请求是可选的匿名统计
- 服务器配置与凭据保存在 Chrome 本地存储中

匿名统计

扩展可能会向 Google Analytics 发送匿名功能使用事件（例如打开扩展、点击刷新 / 重启、查看配置教程）。这些事件只包含功能名称和服务商类型——绝不包含 API Key、凭据、主机名或服务器内容。与任何第三方请求一样，Google 会收到发送该事件所用的网络 IP；我们不会在事件数据中放入任何 IP。你可以在设置中彻底关闭统计。

VPS DASHBOARD 不做的事

本扩展有意聚焦服务商层面的管理。

它不会：
- 在你的 VPS 内安装代理程序
- 持续监控在线状态
- 替代监控平台
- 上传或收集你的 API 凭据、服务器配置

快速开始

首次打开扩展且尚未配置服务器时，会显示引导页：选择你要连接的服务商，设置页会自动打开并已预选对应类型。也可以点「暂时跳过」，稍后手动添加。

手动添加步骤：
1. 打开扩展设置
2. 添加服务器配置
3. 选择服务商类型
4. 填写 API 地址与凭据
5. 测试连接
6. 打开弹窗查看状态并执行操作

各服务商的详细配置说明请参阅扩展内的配置教程。

反馈

欢迎提交功能建议、问题报告和服务商兼容性反馈。可使用扩展内置的反馈链接创建预填好的 GitHub issue。

请勿在公开报告中包含 API Key、密钥、Token、IP 地址、主机名或任何其他敏感信息。
